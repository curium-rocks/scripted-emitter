import { describe, it} from 'mocha';
import { expect } from 'chai';

describe( 'ScriptedEmitterFactory', function() {
    describe( 'buildEmitter()', function() {
        it( 'Should create an emitter to specification', function() {
            expect(false).to.be.true;
        });
    });
    describe( 'recreateEmitter()', function() {
        it( 'Should recreate an emitter to specification from plaintext', function() {
            expect(false).to.be.true;
        });
        it( 'Should recreate an emitter to specification from aes-256-gcm ciphertext', function() {
            expect(false).to.be.true;
        });
    });
});